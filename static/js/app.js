var document_title;$(document).ready(function () {		document_title = document.title;		// fix form bug...	$("form[action='']").attr('action', window.location.href);		if (typeof(G_NOTIFICATION_INTERVAL) != 'undefined')	{		check_notifications();			setInterval('check_notifications()', G_NOTIFICATION_INTERVAL);	}		$('a[rel=lightbox]').fancybox({					openEffect  : 'none',		closeEffect : 'none',		prevEffect : 'none',		nextEffect : 'none',		closeBtn  : false,		helpers : {			buttons	: {				position : 'bottom'			}		},		afterLoad : function() {			this.title = '第 ' + (this.index + 1) + ' 张, 共 ' + this.group.length + ' 张' + (this.title ? ' - ' + this.title : '');		}	});		if (typeof(myMarkdownSettings) != 'undefined' && document.getElementById('advanced_editor'))	{				$('#advanced_editor').markItUp(myMarkdownSettings);				$.setEditorPreview();				setInterval(function()		{		    	if (advanced_editor != null){		    	advanced_editor.save();		        $('#markItUpPreviewFrames').html(Markdown($('#advanced_editor').val()));	    	}		    	var content = $('#advanced_editor').val().split(/\r?\n/);	    		    	if (content.length > 5 && content.length < 30) {	    		$('#advanced_editor').height(content.length * 21).css('overflow', 'hidden');	    	} else if (content.length >= 30) {	    		$('#advanced_editor').height(30 * 21).css('overflow', 'auto');	    	}	    }, 500);	}	else if (document.getElementById('markItUpPreviewFrame'))	{		$('#markItUpPreviewFrame').hide();	}		/*编辑话题按钮*/	show_edit_box('.aw-edit-topic');	/*分享站外操作*/	show_share_box_outside('.alert-share-box-outside');	/*分享私信操作*/	show_share_box_message('.alert-share-box-message');	/*问题重定向*/	show_question_redirect_box('.alert-question-redirect-box');	/*添加评论操作box*/	show_comment_box('.aw-add-comment');	/*提示警告框*/	show_alert_tips_box('.aw-alert-box','被折叠的回复是被大多数用户认为没有帮助的回复!');	/*评论修改框*/	show_edit_comment_box('.aw-alert-edit-comment-box');			/*站内邀请回复操作*/	$('.aw-side-bar-invite-replay .aw-side-bar-mod-body a').click(function(){		if ($('.aw-side-bar-invite-box div').eq($(this).index()).is(':hidden'))		{			$('.aw-side-bar-invite-box div').hide();				$('.aw-side-bar-invite-box div').eq($(this).index()).show();		}else		{			$('.aw-side-bar-invite-box div').eq($(this).index()).hide();		}	});		/*显示更多赞同*/	$('.aw-comment-zantong-show').click(function()	{		$(this).parent().children().show();		$(this).hide();	});		/*添加话题*/	$('.aw-add-topic').click(function()	{		if (!$(this).parent().parent().children().hasClass('aw-edit-topic-box')){			$(this).parent().parent().append(AW_TEMPLATE.editTopicBox);			//给编辑box取消按钮添加事件			$('.aw-edit-topic-box .close-edit').click(function(){				$(this).parent().parent().find('.aw-notopic-sort').show();				$(this).parent().hide();			});			//显示编辑box			$(this).parent().parent().find('.aw-edit-topic-box').fadeIn();		} else		{			$(this).parent().parent().find('.aw-edit-topic-box').fadeIn();		}		/*隐藏话题编辑按钮*/		$(this).parent().hide();	});});